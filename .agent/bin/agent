#!/bin/sh
# agentic-coding shim (engine-owned; regenerated by `agent init`, pins the major).
if [ "${IS_SANDBOX:-}" = "1" ]; then
	echo "agent: already inside the agent container; host shim skipped." >&2
	exit 0
fi
exec npx -y @couetilc/agentic-coding@^0 "$@"
